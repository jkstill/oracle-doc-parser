---
id: 19c__DBMS_XMLGEN.NEWCONTEXTFROMHIERARCHY
name: DBMS_XMLGEN.NEWCONTEXTFROMHIERARCHY
object_type: plsql_function
oracle_version: 19c
doc_type: plsql_packages
parent: DBMS_XMLGEN
tags: [dbms_xmlgen]
source_file: DBMS_XMLGEN.html
---

# DBMS_XMLGEN.NEWCONTEXTFROMHIERARCHY

This function obtains a handle to use in the GETXML Functions and other functions to get a hierarchical XML with recursive elements from the result.

## Syntax

```sql
DBMS_XMLGEN.NEWCONTEXTFROMHIERARCHY ( 
   queryString IN VARCHAR2) 
 RETURN ctxHandle;
```

## Parameters

| Parameter | Type | Mode | Description |
|-----------|------|------|-------------|
| queryString | VARCHAR2) | IN | The query string, the result of which must be converted to XML. The query is a hierarchical query typically formed using a CONNECT BY clause, and the result must have the same property as the result set generated by a CONNECT BY query. The result set must have only two columns, the level number and an XML value. The level number is used to determine the hierarchical position of the XML value within the result XML document. |

**Returns:** `ctxHandle`

## Usage Notes

Syntax DBMS_XMLGEN.NEWCONTEXTFROMHIERARCHY ( queryString IN VARCHAR2) RETURN ctxHandle; Parameters Table 205-8 NEWCONTEXTFROMHIERARCHY Function Parameters Parameter Description queryString The query string, the result of which must be converted to XML. The query is a hierarchical query typically formed using a CONNECT BY clause, and the result must have the same property as the result set generated by a CONNECT BY query. The result set must have only two columns, the level number and an XML value. The level number is used to determine the hierarchical position of the XML value within the result XML document.